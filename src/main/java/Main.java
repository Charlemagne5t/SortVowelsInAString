import java.util.Arrays;

public class Main {
    public static void main(String[] args) {
        char[] chars = {'q', 'e', 'E','a', 'b', 'c', 'A'};
        Arrays.sort(chars);
        System.out.println(Arrays.toString(chars));
    }
}
